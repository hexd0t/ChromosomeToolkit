[package]
name = "formats"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = { version = "2.6", features = ["serde"] }
encoding_rs = "0.8"
serde = { workspace = true }
uuid = { version = "1.11", features = ["serde", "v4"] }
num_enum = "0.7"
#backtrace = "*"